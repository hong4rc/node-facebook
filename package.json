{
  "name": "facebook-api",
  "description": "Fake that you alway online + \"seen\" and some feature.",
  "version": "0.0.1",
  "scripts": {
    "lint": "eslint **/* --fix",
    "autocrlf": "crlf --set=CRLF .* * utils/* src/*",
    "pre-commit": "yarn run autocrlf && yarn run lint"
  },
  "author": "Hongarc",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "console-control-strings": "^1.1.0",
    "request": "^2.87.0"
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "crlf": "^1.1.1"
  },
  "engines": {
    "node": "^10.0.0",
    "yarn": "^1.7.0"
  }
}
